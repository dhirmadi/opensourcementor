# Container/service environment template
# Copy to your service or team folder as .env and customize values

# Registry
REGISTRY_HOST=registry.<IP>.sslip.io
REGISTRY_NAMESPACE=team1
IMAGE_NAME=mentor-bot
TAG=latest

# Routing
TEAM_SUBDOMAIN=team1
HOST_SUFFIX=<IP>.sslip.io

# App
PORT=8080

# LiteMAAS
LITEMAAS_BASE_URL=https://lite-maas.example/api
LITEMAAS_API_KEY=changeme

# Compose
COMPOSE_PROJECT_NAME=mentorbot
